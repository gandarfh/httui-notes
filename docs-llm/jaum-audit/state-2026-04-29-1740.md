# State snapshot — 2026-04-29 17:40 (heavy session)

## What this session shipped (4 substantive commits + 2 docs)

- **`8b704b3`** — split `httui-core/src/db/schema_cache_remote.rs`
  out of `schema_cache.rs` (audit-028). schema_cache.rs lifted
  from audit-027 opt-out at 97.4% coverage.
- **`ff8b28c`** — `pool_manager.rs` opt-out lifted via 12 unit
  tests at 95.3% (cache-only seeding via `#[cfg(test)]
  insert_for_test` helper).
- **`5e13578`** — Epic 41 Story 07 carry slice 1: schema +
  store + tests for `mvp_migration_dismissed`. The remaining
  Story 07 carry (detection hook + AppShell mount +
  migrate_vault_to_v1 dispatch) splits into 3 more slices.
- Plus 2 docs commits (`5b8eb50`, `47d2004`) for the audit-028
  and pool_manager SESSION-LOG entries + state snapshots.

Audit entries written: 028 (schema_cache_remote split), 029
(pool_exec_pg/mysql final disposition — stay excluded until
Epic 32).

## Audit-027 unwind — final state

| File | Initial | Now | Closed by |
|---|---|---|---|
| `lookup.rs` | excluded | 100% | `bf390d6` |
| `vault_stores.rs` | excluded | 99.2% | `bf390d6` |
| `mcp/main.rs` | excluded | 80% | `bf390d6` |
| `GeneralSection.tsx` | excluded | 100% | `bf390d6` |
| `schema_cache.rs` | excluded | 97.4% | `8b704b3` (split) |
| `pool_manager.rs` | excluded | 95.3% | `ff8b28c` (test seed) |
| `pool_exec_pg.rs` | excluded | excluded | Epic 32 (audit-029) |
| `pool_exec_mysql.rs` | excluded | excluded | Epic 32 (audit-029) |
| `schema_cache_remote.rs` (new) | excluded | excluded | Epic 32 (audit-028) |

6 of the original 6 audit-027 files have an answer; 4 lifted
to PASS, 2 stay excluded with audit-029 final disposition.
Plus 1 new narrow opt-out from the audit-028 split.

## Backlog state

Recent tracking:
- **Epic 41 Story 07** — Slice 1 closed (commit 5e13578). Carry
  remaining: detection hook + AppShell mount +
  `migrate_vault_to_v1` Tauri dispatch.
- Other Epic 41 carries unchanged (templates picker, importar
  parser).
- Epic 32 (DB integration test harness) accumulated 3 new
  pointers (audit-027, audit-028, audit-029).

## Quality state

- 642 httui-core tests pass, 25 settings store tests, 998+
  desktop tests.
- All touched-file gates green against `origin/main`.
- 76-commit branch delta against `origin/main`.

## Next iteration entry points

1. **Epic 41 Story 07 carry slice 2 — detection hook**.
   Implement a Rust function (probably in
   `httui-core/src/migrate.rs` or similar) that takes a vault
   path and returns a `MigrationCandidate { has_legacy_db: bool,
   has_v1_layout: bool }`. Plus a Tauri command + a
   `useMigrationDetection(vaultPath)` hook. Pure helpers
   testable; live FS path needs tempdir.
2. **Slice 3 — AppShell mount** of `<MigrationBanner>` gated by
   detection hook + `mvpMigrationDismissed`. Already-shipped
   visual component (commit `95689c4`). Wire is small.
3. **Slice 4 — `migrate_vault_to_v1` Tauri dispatch**. Backend-
   heavy; reads MVP `notes.db` and writes v1 `.httui/*.toml`
   files. Likely the largest slice.
4. Epic 42 (Connections refined UI) — fresh epic if migration
   carry feels stalled.

Recommended next: **#1 (detection hook)** — small, testable,
unblocks slices 3+4.

## Working directory

Clean against committed state. `bun.lock` + `teste.md` +
untracked `connection-kinds.ts` + Tauri schema JSONs dirty
since session start (not authored by me; left alone).
