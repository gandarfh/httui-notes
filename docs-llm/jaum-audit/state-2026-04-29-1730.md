# State snapshot — 2026-04-29 17:30

## What this iteration shipped

- **`ff8b28c`** — `httui-core/src/db/pool_manager.rs` opt-out lifted.
  12 new unit tests at 95.3% file coverage. `#[cfg(test)] pub(crate)
  fn insert_for_test` seeds `PoolEntry`s with
  `DatabasePool::Sqlite(:memory:)` so cache-only paths
  (`invalidate`, `cleanup_expired`, `get_query_timeout`,
  `cleanup_query_log`, cached `get_pool`) are exercised without a
  live PG/MySQL pool.

(Previous iteration shipped `8b704b3` — schema_cache opt-out lift
via `schema_cache_remote.rs` split + audit-028 — and `5b8eb50`
SESSION-LOG entry. That state was captured in
`state-2026-04-29-1700.md`.)

## Backlog state — audit-027 unwind progress

| Audit-027 file | Initial | After bf390d6 | After 8b704b3 | After ff8b28c (now) |
|---|---|---|---|---|
| `lookup.rs` | excluded | 100% PASS | 100% PASS | 100% PASS |
| `vault_stores.rs` | excluded | 99.2% PASS | 99.2% PASS | 99.2% PASS |
| `mcp/main.rs` | excluded | 80% PASS | 80% PASS | 80% PASS |
| `GeneralSection.tsx` | excluded | 100% PASS | 100% PASS | 100% PASS |
| `schema_cache.rs` | excluded | excluded | 97.4% PASS | 97.4% PASS |
| `pool_manager.rs` | excluded | excluded | excluded | **95.3% PASS** |

Remaining audit-027 opt-outs:
- `pool_exec_pg.rs` (148 L, 0%) — almost entirely
  `sqlx::query.execute(pool)` calls; pure helpers are slim.
- `pool_exec_mysql.rs` (254 L, 0%) — same shape.
- `schema_cache_remote.rs` (~50 L, 0%) — narrow scope, audit-028.

Pre-existing opt-outs (untouched this session):
- `httui-tui/src/app.rs` (1753 L; size + coverage; TUI frozen scope, Epic 31)
- `httui-desktop/src/components/editor/MarkdownEditor.tsx` (538 L; audit-022 — Epic 30a Story 01)
- Several Tauri command shells (commands.ts, git.ts, watcher.rs, environments.rs, connections.rs, files.rs, main.rs)

## Quality state

- 652 httui-core tests pass (12 new this iteration).
- All touched-file gates PASS against `origin/main`.
- 73-commit branch delta against origin/main remains.

## Next iteration entry points

1. **`pool_exec_pg.rs` + `pool_exec_mysql.rs`** — these are the
   hardest holdouts. Looking quickly at the file structure: each
   wraps `sqlx::query(sql).fetch_all(&pool)` + per-driver row
   mapping. Pure helpers exist but they're shallow (column
   metadata extraction, JSON conversion). Two strategies:
   - (a) Extract row-mapping into pure functions taking
     test-constructable inputs (mocked Row trait or value sequences).
     Coverage of those helpers + a thin live-DB shell. **Same
     pattern as schema_cache.** Likely yields 60-80% file coverage.
   - (b) Stand up sqlx test fixtures via `#[sqlx::test]` macro
     against the in-memory SQLite pool only — but pool_exec_pg /
     mysql are PG/MySQL specific by definition; macro doesn't help.
   - (c) Wait for Epic 32 testcontainers harness.
   Recommendation: **try (a)** — extract any pure mapping logic
   into a sibling like `pool_exec_pg_mappers.rs` that's testable
   without a live pool, leaving the live shell at audit-027 status.
2. **Epic 41 Story 07 carry — MVP detection hook** (well-bounded
   product feature; matches the original
   `state-2026-05-01-1545.md` recommendation).
3. **Epic 42 (Connections refined UI)** — large fresh epic; canvas
   §5 spec.

Recommended next: **#1 (pool_exec_pg.rs split)** to keep the
audit-027 unwind moving.

## Working directory

Clean against committed state. `bun.lock` + `teste.md` + untracked
`connection-kinds.ts` + Tauri schema JSONs dirty since session
start (not authored by me; left alone).
