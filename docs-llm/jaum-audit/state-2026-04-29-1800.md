# State snapshot — 2026-04-29 18:00

## What this iteration shipped

- **`2be0096`** — Epic 41 Story 07 carry slice 2 (MVP migration
  detection hook end-to-end).
  - Rust: `MigrationCandidate` + `detect_migration_candidate(&Path)`
    in `httui-core/src/vault_config/migration.rs`. 8 detection
    tests + 3 Tauri-command round-trip tests.
  - Tauri command `detect_vault_migration` registered alongside
    existing `migrate_vault_to_v1`.
  - Frontend `lib/tauri/migration.ts` (pure invoke wrapper +
    mirror helper) and `hooks/useMigrationDetection.ts` (combines
    probe with dismissal pref). 8 hook tests.

## Story 07 carry status

| Slice | What | Status |
|---|---|---|
| 1 | `mvp_migration_dismissed` schema + store | DONE — commit `5e13578` |
| 2 | Detection hook (backend + Tauri + React hook) | DONE — commit `2be0096` |
| 3 | AppShell mount of `<MigrationBanner>` | next |
| 4 | Wire `migrate_vault_to_v1` through banner's `onMigrate` | after slice 3 |

`<MigrationBanner>` already shipped at component level (commit
`95689c4` — Epic 41 Story 07 visual). Slice 3 = mount it in
the empty-state screen gated by
`useMigrationDetection(vaultPath).shouldShowBanner`.
Slice 4 = wrap `migrate_vault_to_v1` Tauri command in a
mutation handler that the banner's `onMigrate` calls; on
success, `refresh()` from the detection hook clears the banner.

## Quality state

- 663 httui-core tests pass (8 new), 56 httui-notes tests pass
  (3 new), 1006+ desktop tests (8 new).
- All touched-file gates green against `origin/main`:
  migration.rs 96.0%, vault_config_commands.rs 85.8%,
  useMigrationDetection.ts 100%, migration.ts 100%, user.rs 100%,
  settings.ts 100%.
- 78-commit branch delta against `origin/main`.

## Next iteration entry points

1. **Slice 3 — AppShell mount of `<MigrationBanner>`.** Need to:
   - Find AppShell.tsx (or wherever the empty-state lives) and
     conditionally mount `<MigrationBanner onMigrate={...}
     onDismiss={dismiss}>` when `shouldShowBanner` is true.
   - Tests: render with mocked detection hook in both
     banner-shown and banner-hidden cases.
   Small (~30-50 L of changes).
2. **Slice 4 — wire `migrate_vault_to_v1`.** Tauri command
   exists; just need to call it from the banner's `onMigrate`,
   surface progress / error, and `refresh()` on success.
3. **Pivot to Epic 42** if Story 07 carry feels stalled.

Recommended next: **#1 (slice 3)** — closes the visible part of
Story 07; slice 4 is then a small follow-up.

## Audit-027 unwind — final disposition (recap)

Cf. `state-2026-04-29-1740.md`:
- 4 of 6 audit-027 files lifted from opt-out (lookup,
  vault_stores, mcp/main, GeneralSection, schema_cache,
  pool_manager).
- 2 stay excluded with audit-029 final disposition (pool_exec_pg,
  pool_exec_mysql) until Epic 32 testcontainers.
- 1 new narrow opt-out from audit-028 split
  (schema_cache_remote).

## Working directory

Clean against committed state. `bun.lock` + `teste.md` +
untracked `connection-kinds.ts` + Tauri schema JSONs dirty
since session start (not authored by me; left alone).

> Note: `state-2026-04-29-1755.md` already exists from a
> historical paused-loop session at Epic 13 (audit-006). Kept
> untouched. This snapshot uses the next 5-min slot.
