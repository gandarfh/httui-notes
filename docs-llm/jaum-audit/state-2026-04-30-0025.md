# State snapshot — 2026-04-30 00:25

## What this session shipped (4 substantive Story commits + docs)

- **`0f1fb06`** — Epic 42 Story 01 slice 1 (page scaffold:
  ConnectionsPage 3-col grid + KindSidebar + ListPanel header
  + DetailPanel placeholder; 26 tests).
- **`8ed373d`** — Story 01 slice 2 (real list rows +
  derivations: `<ConnectionListRow>`, `connections-derive.ts`
  pure helpers; 39 tests).
- **`3f70006`** — Story 02 (credentials section in detail
  panel: `<ConnectionDetailCredentials>`, edit/save flow,
  rotate password flow; 19 tests).
- **`a4eaae5`** — Story 03 (schema preview:
  `<ConnectionDetailSchemaPreview>`, hot tables top-5,
  collapsible table tree; audit-031 deferral on PK/FK + row
  counts; 15 tests).

Plus 4 docs commits with SESSION-LOG + epic-file + README
tracking.

## Story 03 audit deferral

`SchemaEntry` doesn't carry PK/FK / row count metadata. Adding
it requires backend introspection-SQL changes per driver +
`schema_cache` migration 012 + new struct fields + frontend
type extension. audit-031 owns the deferral; absorbed into
Story 03a or Epic 28 prerequisites.

## Story 02 + 03 — what's left

Both Stories shipped at the **component level**: pure
presentational with consumer-driven props (callbacks +
state). The actual page mount (where AppShell renders
`<ConnectionsPage>`) + the loader hook (which calls
`listConnections()`, `ensureLoaded(id)`, derives the
`enrichment` array from the store + active env, and routes
the Save/Rotate handlers to `update_connection` + keychain)
are still pending. That mount slice closes Stories 01-03
end-to-end.

## Backlog state

- Epic 42 — Stories 01-03 component-level done; Stories 04-06
  pending; audit-031 carry.
- Epic 41 Story 07 — closed end-to-end (commit a142ff2 last
  session).
- Audit-027 unwind — final disposition per audit-029
  (pool_exec_pg/mysql stay excluded; Epic 32 testcontainers).

## Quality state

- 99 tests in the connections component group (26 + 39 +
  19 + 15), all 100% on each file (page is 95.2% — the
  schema-prop conditional fork lacks a no-state test).
- Workspace: 663 httui-core + 56 httui-notes + 1006+ desktop
  tests pass.
- All gates green against `origin/main`.
- 81-commit branch delta against `origin/main`.

## Next iteration entry points

1. **Story 04 — used-in-runbooks** (canvas spec):
   - Vault grep for blocks with `db-<connection_id>` block type
   - List file:line entries; click to open file at that block
   - Cached on connection select; refreshes on file save
   The vault-walk side needs a Tauri command (probably extending
   the existing `search_content` infra). The presentational list
   is similar shape to ListRows.
2. **Story 05 — footer actions** (Test/Rotate/Duplicate/Delete):
   - Test → `test_connection` Tauri + inline result with latency
   - Rotate → reuse Story 02's rotate flow (already shipped)
   - Duplicate → `create_connection` clone with " (copy)" suffix,
     no password copied
   - Delete → confirm + `delete_connection` + remove keychain
3. **Story 06 — new-connection modal with tabs** (Formulário /
   Connection string / SSH / SSL). 4-tab modal centered on
   dimmed list bg. Bigger than other slices.
4. **Page mount** — wire `<ConnectionsPage>` into AppShell or
   Settings drawer. Closes the consumer wiring carries from
   Stories 01-03.

Recommended next: **#1 (Story 04 used-in-runbooks)** — small,
similar component pattern, no big new dialog.

## Working directory

Clean against committed state. `bun.lock` + `teste.md` dirty
since session start (not authored by me; left alone).
